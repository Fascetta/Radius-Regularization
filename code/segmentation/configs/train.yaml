# Output settings
exp_name: EP-Segformer-b2_RAL1
notes: "Training with RAL=1.0"

# General settings
gpus: [0,1,2,3,4,5]
seed: 1
train_batch_size: 6
debug: False

# General training hyperparameters
ral_initial_alpha: 1.
ral_final_alpha: 1.
radius_conf_loss: 0.
radius_label_smoothing: False

# Training hyperparameters
model: euclidean-poincare
dataset: Cityscapes

# Logging
wandb: True

# Test
# checkpoint_path: "code/segmentation/output/cityscapes/best_epoch-epoch=28_val_mIoU-val_mIoU=42.71.ckpt"
test_batch_size: 6